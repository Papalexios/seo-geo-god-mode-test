# Cloudflare Pages configuration for SEO GEO GOD MODE
# SOTA production grade - Quantum SOTA upgrade

name = "seo-geo-god-mode"

# Pages build output directory
pages_build_output_dir = "dist"

# Compatibility settings for Cloudflare Workers
compatibility_date = "2025-01-15"
compatibility_flags = ["nodejs_compat"]

# Production environment configuration
[env.production]
name = "seo-geo-god-mode-prod"

# Preview environment configuration
[env.preview]
name = "seo-geo-god-mode-preview"
